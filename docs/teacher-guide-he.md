# RecursiQuest — מדריך למורה

<div dir="rtl">

> **מה זה RecursiQuest?**
> RecursiQuest הוא כלי ויזואליזציה אינטראקטיבי שלב-אחר-שלב, שנועד לעזור לתלמידים *לראות* ו*להבין* כיצד עובדות אלגוריתמים של רקורסיה ומיון — שלב אחד בכל פעם, עם אנימציות, הסברים בשפה פשוטה וחידון מובנה.

---

## תוכן עניינים

<ol dir="rtl">
<li><a href="#1-סקירה-כללית">סקירה כללית</a></li>
<li><a href="#2-ניווט--שתי-המסכים">ניווט — שתי המסכים</a></li>
<li><a href="#3-קטגוריות-האלגוריתמים">קטגוריות האלגוריתמים</a>
  <ul>
    <li><a href="#רקורסיה">רקורסיה</a></li>
    <li><a href="#מיון">מיון</a></li>
    <li><a href="#הפונקציות-שלי">הפונקציות שלי</a></li>
  </ul>
</li>
<li><a href="#4-לוחות-הויזואליזציה">לוחות הויזואליזציה</a>
  <ul>
    <li><a href="#לוח-מחסנית-הקריאות">מחסנית הקריאות</a></li>
    <li><a href="#לוח-עץ-הרקורסיה">עץ הרקורסיה</a></li>
    <li><a href="#לוח-קטע-הקוד">קטע הקוד</a></li>
    <li><a href="#לוח-הסבר-הקריאה--פרשנות">הסבר הקריאה</a></li>
    <li><a href="#מציג-המערך-למיון-בלבד">מציג המערך</a></li>
    <li><a href="#לוח-תובנה-על-האלגוריתם">תובנה על האלגוריתם</a></li>
  </ul>
</li>
<li><a href="#5-בקרי-הפעלה">בקרי הפעלה</a></li>
<li><a href="#6-מצב-למידה-מול-מצב-חידון">מצב למידה מול מצב חידון</a></li>
<li><a href="#7-הישגים-ורמות">הישגים ורמות</a></li>
<li><a href="#8-הוספת-פונקציה-משלך">הוספת פונקציה משלך</a></li>
<li><a href="#9-ייבוא-וייצוא-פונקציות">ייבוא וייצוא פונקציות</a></li>
<li><a href="#10-תמיכה-בשפות">תמיכה בשפות</a></li>
<li><a href="#11-תכונת-צילום-מסך">תכונת צילום מסך</a></li>
<li><a href="#12-שיתוף-שלב-ספציפי">שיתוף שלב ספציפי</a></li>
<li><a href="#13-קיצורי-מקלדת">קיצורי מקלדת</a></li>
<li><a href="#14-רשימת-תיוג-מהיר-למורים">רשימת תיוג מהיר למורים</a></li>
</ol>

---

## 1. סקירה כללית

RecursiQuest הופך מושג מופשט — "הפונקציה קוראת לעצמה!" — למשהו שתלמידים יכולים ממש לראות. כאשר תלמיד מריץ אלגוריתם, האפליקציה מתעדת כל קריאת פונקציה וכל ערך מוחזר, ואז מאפשרת לתלמיד לעבור ביניהם בכל מהירות באמצעות לוחות אנימציה.

**למי זה מיועד:** תלמידים שלומדים רקורסיה ומיון לראשונה, בדרך כלל בקורס מבוא לתכנות או מבני נתונים.

**שפת דוגמאות הקוד:** C# — נקי, קריא ומוכר לתלמידים הלומדים תכנות מונחה-עצמים.

---

## 2. ניווט — שתי המסכים

### מסך הבית
כאן מתחיל התלמיד. המסך מציג שלוש לשוניות: **רקורסיה**, **מיון**, ו**הפונקציות שלי**.

- כל לשונית מציגה את האלגוריתמים הזמינים כקלפים.
- כל קלף מציג את שם האלגוריתם, תיאור קצר ורמת קושי (🟢 קל / 🟡 בינוני / 🔴 קשה).
- התלמיד בוחר אלגוריתם, מגדיר את הקלט (למשל "n = 5"), ואז לוחץ על **▶ הפעל ויזואליזציה**.
- בורר השפה (EN / عر / עב) זמין בפינה הימנית העליונה של שני המסכים.

### מסך הויזואליזציה
לאחר לחיצה על הפעלה, התלמיד עובר למסך הויזואליזציה שם נמצאים כל לוחות הלמידה. המסך מחולק לעמודות ניתנות לשינוי גודל — התלמידים יכולים לגרור את המחיצות כדי להגדיל או להקטין כל לוח.

---

## 3. קטגוריות האלגוריתמים

### רקורסיה

חמישה אלגוריתמים רקורסיביים קלאסיים, כל אחד מעמיק יותר מבחינה מושגית:

<table dir="rtl" style="width:100%">
<thead><tr><th>אלגוריתם</th><th>תיאור</th><th>קושי</th></tr></thead>
<tbody>
<tr><td><strong>עצרת (Factorial)</strong></td><td>מחשב n! = n × (n−1) × … × 1. הרקורסיה הלינארית הפשוטה ביותר.</td><td>🟢 קל</td></tr>
<tr><td><strong>חזקה (Power)</strong></td><td>מעלה בסיס בחזקה תוך שימוש ברקורסיה: <code>Power(base, exp)</code></td><td>🟢 קל</td></tr>
<tr><td><strong>פיבונאצ'י (Fibonacci)</strong></td><td>מחשב F(n) בשתי קריאות רקורסיביות — מציג רקורסיה מסתעפת ומושג החישוב החוזר.</td><td>🟡 בינוני</td></tr>
<tr><td><strong>סכום מערך (Sum Array)</strong></td><td>מסכום את כל איברי מערך שלמים רקורסיבית.</td><td>🟡 בינוני</td></tr>
<tr><td><strong>פריט מקסימלי (Max Item)</strong></td><td>מוצא את האיבר הגדול ביותר במערך שלמים רקורסיבית.</td><td>🟡 בינוני</td></tr>
</tbody>
</table>

**רצף מוצע:** התחל בעצרת, עבור לחזקה, אחר כך לפיבונאצ'י (נקודת פתיחה מצוינת לדיון מדוע הפונקציה קוראת לעצמה פעמיים), ולאחר מכן לגרסאות המערך.

---

### מיון

שלושה אלגוריתמי מיון המדגימים יחד אסטרטגיות שונות:

<table dir="rtl" style="width:100%">
<thead><tr><th>אלגוריתם</th><th>תיאור</th><th>קושי</th></tr></thead>
<tbody>
<tr><td><strong>מיון בועות (Bubble Sort)</strong></td><td>משווה שוב ושוב איברים סמוכים ומחליף אותם אם הם בסדר הלא נכון. קל לראות כיצד האיבר הגדול "צף" לקצה.</td><td>🟢 קל</td></tr>
<tr><td><strong>מיון בחירה (Selection Sort)</strong></td><td>מוצא שוב ושוב את האיבר המינימלי ומציב אותו בקדמה. אסטרטגיה ברורה במקום.</td><td>🟡 בינוני</td></tr>
<tr><td><strong>מיון הכנסה (Insertion Sort)</strong></td><td>בונה את החלק הממוין איבר אחד בכל פעם, מכניס כל איבר חדש למיקומו הנכון.</td><td>🟡 בינוני</td></tr>
</tbody>
</table>

באלגוריתמי המיון, עץ הרקורסיה מוחלף ב**מציג המערך** — כרטיסיות צבעוניות מונפשות המציגות אילו איברים מושווים ומוחלפים.

---

### הפונקציות שלי

תלמידים (או מורים) יכולים לכתוב ולהציג פונקציות C# רקורסיביות **משלהם**. ראה [סעיף 8](#8-הוספת-פונקציה-משלך) לפרטים מלאים.

---

## 4. לוחות הויזואליזציה

בעת הרצת אלגוריתם, מופיעים על המסך עד חמישה לוחות. כולם מסונכרנים — התקדמות שלב אחד מעדכנת כל לוח בו-זמנית.

---

### לוח מחסנית הקריאות

**מה הוא מציג:** מצב *חי* של מחסנית הקריאות — בדיוק אילו קריאות פונקציה פתוחות כעת (עדיין לא החזירו ערך), מסודרות מהתחתית (קריאה ראשונה) לראש (הקריאה האחרונה).

**כיצד לקרוא:**
- כל מסגרת מציגה את שם הפונקציה וערכי הפרמטרים הנוכחיים שלה (למשל `Factorial(n=3)`).
- המסגרות מקודדות בצבע לפי עומק — הקריאה הראשונה בכחול, הבאה בתכלת, אחר כך ירוק, צהוב, כתום, ואדום לרמות עמוקות יותר.
- כאשר פונקציה מחזירה ערך, המסגרת שלה נעלמת מהמחסנית עם אנימציה חלקה.
- אם מסגרת מציגה תווית **BASE CASE**, התלמיד עד להתנאי העצירה של הרקורסיה.

**שימוש הוראתי:** שאל תלמידים "כמה מסגרות נמצאות במחסנית כעת?" כדי לבנות אינטואיציה לגבי עומק הקריאה וזיכרון המחסנית.

---

### לוח עץ הרקורסיה

**מה הוא מציג:** תרשים עץ גדל המציג את כל קריאות הפונקציה עד כה, מסודר כך שילדיו של כל צומת הם הקריאות הרקורסיביות שהוא ביצע.

**כיצד לקרוא:**
- כל מעגל (צומת) מייצג קריאת פונקציה אחת עם ערכי הקלט שלה.
- קווים בין הצמתים מציגים "קריאה זו הפעילה את הקריאה הבן."
- הצמתים מאירים עם ערך ההחזרה שלהם ברגע שהפונקציה מחזירה.
- צמתי מקרה הבסיס מודגשים אחרת כדי לסמן בבירור היכן הרקורסיה נעצרה.

**שימוש הוראתי:** בפיבונאצ'י, העץ מציג בבירור *אותה תת-בעיה מחושבת מספר פעמים* — ויזואל עוצמתי להנעת מושג ה-Memoization בהמשך הקורס. בעצרת ובחזקה, העץ הוא קו ישר, המחזק את מושג "הרקורסיה הלינארית."

---

### לוח קטע הקוד

**מה הוא מציג:** קוד המקור המלא ב-C# של האלגוריתם הנבחר, עם **הדגשת שורה חיה** — השורה המבוצעת כעת מודגשת בכחול עם סמן גבול שמאלי.

**כיצד להשתמש:**
- ניתן לקפל ולהרחיב את הלוח על ידי לחיצה על כותרתו.
- הדגשת התחביר מצבעת מילות מפתח (סגול), מספרים (כתום), מחרוזות (ירוק) ותגובות (אפור).
- כאשר התלמידים מתקדמים שלב אחר שלב, ההדגשה זזה בתוך הקוד, מקשרת את מה שנראה בעץ לשורת הקוד בפועל.

**שימוש הוראתי:** מצוין לפעילויות בזוגות מסוג "אני רואה את הקריאה בעץ — איזו שורת קוד יצרה אותה?"

---

### לוח הסבר הקריאה / פרשנות

**מה הוא מציג:** משפט בשפה פשוטה המסביר מה קורה בשלב הנוכחי. לכל סוג שלב יש סגנון הסבר משלו:

<table dir="rtl" style="width:100%">
<thead><tr><th>סוג שלב</th><th>דוגמת הסבר</th></tr></thead>
<tbody>
<tr><td><strong>קריאה (CALL)</strong></td><td><em>"קריאה ל-Factorial(n=3) בעומק 1. הוא יקרא ל-Factorial(n=2) בשלב הבא."</em></td></tr>
<tr><td><strong>מקרה בסיס (BASE CASE)</strong></td><td><em>"הגיע למקרה הבסיס! Factorial(n=1) מחזיר 1 מיד — כאן הרקורסיה נעצרת."</em></td></tr>
<tr><td><strong>החזרה (RETURN)</strong></td><td><em>"Factorial(n=3) מחזיר 6 (= 3 × 2)."</em></td></tr>
<tr><td><strong>השוואה (COMPARE)</strong></td><td><em>"משווה מיקומים 2 ו-3: ערך 8 > ערך 1, תתבצע החלפה."</em></td></tr>
<tr><td><strong>החלפה (SWAP)</strong></td><td><em>"מחליף מיקומים 2 ו-3. המערך הוא כעת [5, 3, 1, ...]."</em></td></tr>
</tbody>
</table>

כפתור **"עוד ▼"**** מרחיב את ההסבר להצגת הסבר עמוק יותר של *מדוע* שלב זה מתרחש.

**שימוש הוראתי:** תלמידים יכולים לקרוא את ההסבר בקול רם בכיתה, או להשתמש ב"עוד" לבדיקה עצמית של ההבנה בין שלבים.

---

### מציג המערך (למיון בלבד)

**מה הוא מציג:** המערך הממוין כשורה של כרטיסיות מונפשות. כל כרטיסייה מציגה את ערכה ואמוג'י פרצוף שמשתנה כדי לשקף את מצבה:

<table dir="rtl" style="width:100%">
<thead><tr><th>מצב</th><th>אמוג'י</th><th>משמעות</th></tr></thead>
<tbody>
<tr><td>ברירת מחדל</td><td>🙂</td><td>איבר במנוחה</td></tr>
<tr><td>השוואה</td><td>😮</td><td>האיבר מושווה כעת</td></tr>
<tr><td>החלפה</td><td>😲</td><td>האיבר מוחלף כעת</td></tr>
<tr><td>ממוין</td><td>✅</td><td>האיבר הגיע למיקום הסופי שלו</td></tr>
</tbody>
</table>

תווית טקסט קטנה מתחת לכרטיסיות מאשרת את תוצאת ההשוואה (למשל "8 > 1 — תהיה החלפה").

---

### לוח תובנה על האלגוריתם

**מה הוא מציג:** לוח צדדי ניתן לקיפול עם:
- **הנוסחה המתמטית** של האלגוריתם (למשל `F(n) = F(n-1) + F(n-2)`).
- **תיאור** של מה שהאלגוריתם עושה מבחינה מושגית.
- **מחלקת סיבוכיות הזמן** הנוכחית (למשל O(n)‏, O(2ⁿ)).
- **שרשרת חישוב** חיה הנבנית שלב אחר שלב (למשל `4! = 4 × 3 × 2 × 1`), או עבור פיבונאצ'י, סדרת הערכים המחושבים והערה על כמה פעמים כל תת-בעיה חושבה מחדש.

---

## 5. בקרי הפעלה

סרגל הבקרה בתחתית מסך הויזואליזציה מספק שליטה מלאה בניווט:

<table dir="rtl" style="width:100%">
<thead><tr><th>בקרה</th><th>פעולה</th></tr></thead>
<tbody>
<tr><td><strong>⏮ איפוס</strong></td><td>קפיצה חזרה לשלב 1</td></tr>
<tr><td><strong>◄ אחורה</strong></td><td>התקדמות שלב אחת אחורה</td></tr>
<tr><td><strong>► הפעל / ⏸ השהה</strong></td><td>הפעלה אוטומטית במהירות הנבחרת</td></tr>
<tr><td><strong>► הבא</strong></td><td>התקדמות שלב אחת קדימה</td></tr>
<tr><td><strong>⏭ דלג לסוף</strong></td><td>קפיצה לשלב האחרון מיד</td></tr>
<tr><td><strong>סרגל התקדמות</strong></td><td>לחץ בכל מקום בסרגל לקפיצה ישירה לאותו שלב</td></tr>
<tr><td><strong>בורר מהירות</strong></td><td>בחר מ-0.25×‏, 0.5×‏, 1×‏, 2× או 4×</td></tr>
<tr><td><strong>מונה שלבים</strong></td><td>מציג "שלב 7 / 23" כך שהתלמיד תמיד יודע היכן הוא</td></tr>
</tbody>
</table>

---

## 6. מצב למידה מול מצב חידון

### מצב למידה (ברירת מחדל)
התלמיד עובר דרך האלגוריתם בחופשיות, קורא את הפרשנות וצופה בעדכון הלוחות. ללא לחץ — הוא יכול להתקדם ולחזור כרצונו.

### מצב חידון
נגיש דרך כפתור **🎯 מצב חידון** בכותרת.

במצב חידון, לפני שכל שלב מתגלה, מוצגת לתלמיד **שאלת רב-ברירה** המבקשת ממנו לחזות מה יקרה בשלב הבא. לאחר המענה:
- ✅ תשובות נכונות מציגות הדגשה ירוקה עם הסבר על *מדוע* זה נכון.
- ❌ תשובות שגויות מציגות את הבחירה הנכונה עם הסבר.

**מערכת XP וקומבו:**
- כל תשובה נכונה מרוויחה נקודות XP.
- קבלת 3 תשובות נכונות או יותר ברצף מציגה מכפיל קומבו 🔥.
- הגעה לקומבו של 5 פותחת את ההישג **"אתה בשיא ביצועיך!"**

התלמידים יכולים לצאת ממצב החידון בכל עת ולחזור למצב למידה.

---

## 7. הישגים ורמות

תלמידים מרוויחים **הישגים** בתוך האינטראקציה עם הכלי. ההישגים שמורים בדפדפן כך שהתקדמות נמשכת בין הפגישות.

<table dir="rtl" style="width:100%">
<thead><tr><th>הישג</th><th>כיצד להשיג</th></tr></thead>
<tbody>
<tr><td>🛑 <strong>מקרה הבסיס הראשון!</strong></td><td>הגעה לשלב BASE_CASE בכל אלגוריתם</td></tr>
<tr><td>🌀 <strong>כובש הפיבונאצ'י</strong></td><td>השלמת ויזואליזציה מלאה של פיבונאצ'י</td></tr>
<tr><td>↕️ <strong>אלוף המיון</strong></td><td>צפייה באלגוריתם מיון עד השלמתו</td></tr>
<tr><td>🔥 <strong>אתה בשיא ביצועיך!</strong></td><td>קבלת 5 תשובות נכונות ברצף</td></tr>
<tr><td>💥 <strong>שרד את ה-Overflow</strong></td><td>צפייה במסך אסון ה-stack overflow</td></tr>
</tbody>
</table>

**מערכת רמות:** פתיחת הישגים מעלה את רמת התלמיד. הרמה הנוכחית ותוויתה מוצגות במסך הבית, מה שמעניק תחושה קלה של התקדמות.

בכל פעם שהישג חדש נפתח, מוצגת **הודעת טוסט** בפינה עם כותרת ההישג ואיקון שלו.

---

## 8. הוספת פונקציה משלך

לשונית **הפונקציות שלי** מאפשרת לכל אחד להוסיף פונקציית C# רקורסיבית מותאמת לכלי. זה חזק למורים שרוצים להדגים דוגמאות משלהם, או לתלמידים שרוצים להתנסות.

### כיצד להוסיף פונקציה

1. במסך הבית, לחץ על לשונית **הפונקציות שלי**.
2. לחץ על קלף **➕ הוסף את הפונקציה שלך**.
3. בתיבת הדו-שיח שנפתחת, הדבק או הקלד פונקציית C# רקורסיבית. הפונקציה חייבת:
   - להחזיר `int`‏, `long`‏, `double` או `float`.
   - לקבל רק פרמטרים מספריים פשוטים (`int`‏, `long`‏, `double`‏, `float`).
   - לכלול מקרה בסיס (למשל `if (n <= 1) return 1;`).
   - לקרוא לעצמה רקורסיבית.

**דוגמה:**
<div dir="ltr">

```csharp
int Power(int base, int exp)
{
    if (exp == 0) return 1;
    return base * Power(base, exp - 1);
}
```

</div>

4. לחץ **בדוק** — האפליקציה תנתח את הפונקציה ותריץ מעקב מהיר. הודעה ירוקה מאשרת כמה שלבים וקריאות נוצרו: `✅ Power(base=4, exp=4) — 10 שלבים, 5 קריאות, 5 החזרות. מוכן להוספה!`
5. אם יש שגיאה, הודעה מסבירה מה השתבש (למשל סוג החזרה לא נתמך, מקרה בסיס חסר).
6. לחץ **הוסף** לשמירת הפונקציה.

### ניהול פונקציות מותאמות

- פונקציות מותאמות מופיעות כקלפים בלשונית **הפונקציות שלי** בדיוק כמו אלגוריתמים מובנים.
- רחף מעל קלף פונקציה מותאמת לראות כפתורי **עריכה** ו**מחיקה**.
- הפונקציה שמורה באחסון המקומי של הדפדפן ונמשכת בין טעינות עמוד.

---

## 9. ייבוא וייצוא פונקציות

ניתן לשמור פונקציות מותאמות בקובץ ולשתף אותן — שימושי אם מורה רוצה שכל התלמידים יהיו עם אותה מגוון פונקציות, או אם תלמיד רוצה להעביר פונקציות שלו לדפדפן או מחשב אחר.

### ייצוא

1. במסך הבית, לחץ על כפתור **📥 ייצא פונקציות** (מוצג כאשר קיימת לפחות פונקציה מותאמת אחת).
2. קובץ JSON בשם `recursiquest-functions-YYYY-MM-DD.json` מורד למכשיר.

### ייבוא

1. לחץ על כפתור **📤 ייבא פונקציות**.
2. בחר קובץ `.json` שיוצא בעבר.
3. האפליקציה מייבאת את כל הפונקציות מהקובץ ומשלבת אותן עם פונקציות מותאמות קיימות. מזהים כפולים מוחלפים בגרסה המיובאת.
4. פונקציות מיובאות מופיעות מיד בלשונית **הפונקציות שלי**.

**תרחיש הוראתי:** מורה מכין 3 פונקציות מותאמות, מייצא את הקובץ ומשתף אותו עם הכיתה. כל תלמיד מייבא את הקובץ ושלוש הפונקציות מוכנות מיידית לויזואליזציה.

---

## 10. תמיכה בשפות

RecursiQuest תומך בשלוש שפות ממשק, הניתנות להחלפה בכל עת עם בורר השפה בפינה הימנית העליונה של שני המסכים:

<table dir="rtl" style="width:100%">
<thead><tr><th>קוד</th><th>שפה</th><th>כיוון כתיבה</th></tr></thead>
<tbody>
<tr><td><strong>EN</strong></td><td>אנגלית</td><td>משמאל לימין</td></tr>
<tr><td><strong>عر</strong></td><td>ערבית</td><td>מימין לשמאל (RTL)</td></tr>
<tr><td><strong>עב</strong></td><td>עברית</td><td>מימין לשמאל (RTL)</td></tr>
</tbody>
</table>

החלפת שפה מעדכנת **את כל הטקסט** בממשק — לשוניות, כפתורים, פרשנויות, שאלות חידון, הודעות שגיאה וכל תוויות הלוחות — מיד, ללא טעינה מחדש של הדף. הפריסה מתהפכת אוטומטית לשפות RTL.

לוח קטע הקוד תמיד מציג קוד C# משמאל לימין ללא קשר לשפה הנבחרת, מכיוון שקוד תכנות ניטרלי מבחינת כיוון.

---

## 11. תכונת צילום מסך

לכל אחד מהלוחות הראשיים — **מחסנית הקריאות** ו**עץ הרקורסיה** — יש כפתור 📷 **צילום מסך** קטן בפינה הימנית העליונה שלו. לחיצה עליו שומרת תמונת PNG של הלוח בתיקיית ה-Downloads של המכשיר.

הקובץ מקבל שם אוטומטי, למשל `callstack-factorial-step7.png` או `recursiontree-fibonacci-step12.png`.

**שימוש הוראתי:** תלמידים יכולים לצלם תמונת עץ הרקורסיה בנקודה העמוקה ביותר שלו ולכלול אותה בדוח שיעורי בית או להציג אותה בכיתה.

---

## 12. שיתוף שלב ספציפי

כפתור **🔗 העתק קישור** בסרגל הבקרה מעתיק את כתובת ה-URL של הדף הנוכחי — עם מספר השלב המקודד בדיוק — ללוח. שיתוף כתובת ה-URL הזו מוביל כל אחד ישירות לאותו אלגוריתם באותו שלב.

**שימוש הוראתי:** מורה יכול לנווט לרגע מרכזי (למשל שלב 8 של פיבונאצ'י כאשר שני הענפים פתוחים), להעתיק את הקישור ולשתף אותו עם הכיתה כדי שכולם יתחילו בדיוק באותו רגע.

---

## 13. קיצורי מקלדת

תלמידים המעדיפים ניווט מקלדת יכולים להשתמש בקיצורים הבאים במסך הויזואליזציה:

<table dir="rtl" style="width:100%">
<thead><tr><th>מקש</th><th>פעולה</th></tr></thead>
<tbody>
<tr><td><code>←</code> (חץ שמאלה)</td><td>שלב קודם</td></tr>
<tr><td><code>→</code> (חץ ימינה)</td><td>שלב הבא</td></tr>
<tr><td><code>Space</code> (רווח)</td><td>הפעל / השהה</td></tr>
<tr><td><code>R</code></td><td>איפוס לתחילה</td></tr>
</tbody>
</table>

---

## 14. רשימת תיוג מהיר למורים

השתמש ברשימה זו בעת הצגת RecursiQuest לכיתה:

- [ ] פתח את האפליקציה והצג את מסך הבית עם **שלוש הלשוניות**.
- [ ] בחר **Factorial** (קל), הגדר `n = 4`, לחץ **▶ הפעל ויזואליזציה**.
- [ ] הצבע על **מחסנית הקריאות** הנבנית עם התקדמות השלבים.
- [ ] הצבע על **קטע הקוד** — צפה בתנועת השורה המודגשת.
- [ ] קרא את **הפרשנות** בקול רם עם הכיתה לכל שלב.
- [ ] במקרה הבסיס, שאל: *"מדוע הרקורסיה נעצרת כאן?"*
- [ ] השתמש ב**הפעלה** במהירות 0.5× להדגמה חלקה.
- [ ] עבור ל-**Fibonacci(5)** — הצג כיצד **עץ הרקורסיה** מסתעף.
- [ ] עבור ל-**Bubble Sort** — הצג את **מציג המערך**.
- [ ] החלף שפה ל**ערבית או עברית** להדגמת תמיכת RTL.
- [ ] הדגם הוספת **פונקציה מותאמת** באמצעות דוגמה מהכיתה.
- [ ] הפעל **מצב חידון** ובקש מהתלמידים לחזות את השלב הבא.
- [ ] הצג **ייצוא** והסבר כיצד תלמידים יכולים לקחת את עבודתם הביתה.

---

*RecursiQuest — אפריל 2026*

</div>
